import React from "react";
import { Box, Typography } from "@mui/material";

export default function Auth() {
    return (
        <Box>
            <Typography variant="h4" gutterBottom>
                Autenticação
            </Typography>
            <Typography variant="body1">
                Página de login / autenticação (placeholder).
            </Typography>
        </Box>
    );
}
